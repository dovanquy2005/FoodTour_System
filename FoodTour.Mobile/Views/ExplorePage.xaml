<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:FoodTour.Mobile.Models"
             xmlns:viewmodels="clr-namespace:FoodTour.Mobile.ViewModels"
             x:Class="FoodTour.Mobile.Views.ExplorePage"
             x:DataType="viewmodels:ExploreViewModel"
             BackgroundColor="{StaticResource Background}"
             NavigationPage.HasNavigationBar="False">

	<Grid RowDefinitions="Auto, Auto, *" Margin="20, 15, 20, 0">

		<!-- ═══════ HEADER ═══════ -->
		<VerticalStackLayout Grid.Row="0" Margin="0,12,0,18" HorizontalOptions="Center" Spacing="2">
			<Label Text="Vinh Khanh Street Eats"
			       TextColor="{StaticResource TextPrimary}"
			       FontSize="22"
			       FontAttributes="Bold"
			       HorizontalOptions="Center"/>
			<Label Text="Khám phá ẩm thực đường phố"
			       TextColor="{StaticResource TextSecondary}"
			       FontSize="13"
			       HorizontalOptions="Center"/>
		</VerticalStackLayout>

		<!-- ═══════ THANH TÌM KIẾM ═══════ -->
		<Border Grid.Row="1"
		        BackgroundColor="{StaticResource Surface}"
		        StrokeShape="RoundRectangle 16"
		        Stroke="{StaticResource CardBorder}"
		        StrokeThickness="1"
		        HeightRequest="48"
		        Margin="0,0,0,18"
		        Shadow="{Shadow Radius=6, Opacity=0.06, Offset='0,2'}">
			<Grid ColumnDefinitions="Auto, *" Margin="14,0,14,0">
				<Image Grid.Column="0"
				       Source="search.png"
				       WidthRequest="20" HeightRequest="20"
				       VerticalOptions="Center"
				       Margin="0,0,10,0"
				       Opacity="0.45"/>
				<Entry Grid.Column="1"
				       Placeholder="Tìm ốc, hải sản, lẩu..."
				       PlaceholderColor="{StaticResource TextTertiary}"
				       TextColor="{StaticResource TextPrimary}"
				       BackgroundColor="Transparent"
				       VerticalOptions="Center"
				       FontSize="15"/>
			</Grid>
		</Border>

		<!-- ═══════ DANH SÁCH QUÁN ĂN ═══════ -->
		<CollectionView Grid.Row="2"
		                ItemsSource="{Binding Shops}"
		                SelectionMode="Single"
		                SelectionChanged="OnShopSelected">
			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="models:ShopModel">
					<Border BackgroundColor="{StaticResource Surface}"
					        StrokeShape="RoundRectangle 16"
					        Stroke="{StaticResource CardBorder}"
					        StrokeThickness="1"
					        Margin="0,0,0,14"
					        Shadow="{Shadow Radius=8, Opacity=0.06, Offset='0,3'}">
						<Grid RowDefinitions="175, Auto">

							<!-- Ảnh quán + bo góc trên -->
							<Border Grid.Row="0"
							        StrokeThickness="0"
							        StrokeShape="RoundRectangle 16,16,0,0"
							        BackgroundColor="{StaticResource Gray100}">
								<Image Source="{Binding ImageUrl}" Aspect="AspectFill"/>
							</Border>

							<!-- Badge rating -->
							<Border Grid.Row="0"
							        BackgroundColor="#E6FFFFFF"
							        StrokeShape="RoundRectangle 10"
							        StrokeThickness="0"
							        HorizontalOptions="End"
							        VerticalOptions="Start"
							        Margin="0,10,10,0"
							        Padding="9,5">
								<HorizontalStackLayout Spacing="4" VerticalOptions="Center">
									<Label Text="★" TextColor="{StaticResource Primary}" FontSize="13" VerticalOptions="Center"/>
									<Label Text="{Binding Rating}" TextColor="{StaticResource TextPrimary}" FontSize="12" FontAttributes="Bold" VerticalOptions="Center"/>
								</HorizontalStackLayout>
							</Border>

							<!-- Thông tin quán -->
							<VerticalStackLayout Grid.Row="1" Margin="14,12,14,14" Spacing="3">
								<Label Text="{Binding Name}"
								       TextColor="{StaticResource TextPrimary}"
								       FontSize="17"
								       FontAttributes="Bold"
								       LineBreakMode="TailTruncation"
								       MaxLines="1"/>
								<Label Text="{Binding Address}"
								       TextColor="{StaticResource TextSecondary}"
								       FontSize="13"
								       LineBreakMode="TailTruncation"
								       MaxLines="1"/>
							</VerticalStackLayout>

						</Grid>
					</Border>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>

	</Grid>
</ContentPage>